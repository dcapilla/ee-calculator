{
  "name": "ee-calculator",
  "private": true,
  "version": "1.0.0",
  "description": "Equal Experts calculator",
  "devDependencies": {
    "angular": "1.6.9",
    "angular-mocks": "1.6.9",
    "http-server": "0.11.1",
    "jasmine-core": "^3.2.0",
    "karma": "^0.13.22",
    "karma-coverage": "^1.1.2",
    "karma-jasmine": "^0.2.0",
    "karma-phantomjs-launcher": "^1.0.4"
  },
  "scripts": {
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1",
    "pretest": "npm install",
    "test": "karma start karma.conf.js",
    "test-single-run": "karma start karma.conf.js  --single-run"
  }
}
